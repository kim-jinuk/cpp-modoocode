// 파일 삭제하기 (std::filesystem::remove 함수 사용)
#include <iostream>
#include <filesystem>

namespace fs = std::filesystem;

int main() {
    // fs::path p("./a/a.txt");

    // std::cout << "Does " << p << " exist? [" << std::boolalpha << fs::exists(p) << "]\n";
    // fs::remove(p);
    // std::cout << "Does " << p << " exist? [" << std::boolalpha << fs::exists(p) << "]\n";

    return 0;
}